# Long-lived token for mcp-remote. Cluster populates .data.token.
apiVersion: v1
kind: Secret
metadata:
  name: mcp-remote-token
  namespace: default
  annotations:
    kubernetes.io/service-account.name: "mcp-remote"
type: kubernetes.io/service-account-token
