# =============================================================================
# Llama Stack Examples - Shared Configuration
# =============================================================================
# This is the shared configuration file for all examples.
# Copy this file to .env and edit with your values:
#   cp env.template .env
#
# All examples will load this file from the examples/ directory.
# =============================================================================

# =============================================================================
# Llama Stack Server Configuration
# =============================================================================

# Llama Stack URL (used by all examples)
# Both REMOTE_BASE_URL and LLAMA_STACK_URL point to the same server
REMOTE_BASE_URL=https://llama-stack-example-llama-stack-example.apps.ocp.sandbox.example.com
LLAMA_STACK_URL=${REMOTE_BASE_URL}

# Inference Model ID
INFERENCE_MODEL_ID=llama-4-scout-17b-16e-w4a16
MODEL_ID=${INFERENCE_MODEL_ID}

# Embedding Model (must match server config)
EMBEDDING_MODEL=granite-embedding-125m

# =============================================================================
# SSL Configuration
# =============================================================================

# Set to true to verify SSL certificates
VERIFY_SSL=false
SKIP_SSL_VERIFY=true

# Timeout in seconds
LLAMA_STACK_TIMEOUT=300

# =============================================================================
# Vector Store Configuration
# =============================================================================
# Each example has its own vector store with different documents.
# Leave empty to create a new vector store on first run.

# Vector Store for rag-mcp-chatbot (discounts application docs)
MCP_VECTOR_STORE_ID=

# Vector Store for rag-evaluation-ragas (Millbrook evaluation dataset)
RAGAS_VECTOR_STORE_ID=

# =============================================================================
# RAG MCP Chatbot - Specific Configuration
# =============================================================================

# Documents directory for chatbot (relative to rag-mcp-chatbot/)
MCP_DOCUMENTS_DIR=documents

# Test Query (optional)
# TEST_QUERY="list pods in openshift-gitops and explain discounts troubleshooting"

# =============================================================================
# RAG Evaluation RAGAS - Specific Configuration
# =============================================================================

# Documents directory for evaluation (relative to rag-evaluation-ragas/)
RAGAS_DOCUMENTS_DIR=documents

# Dataset paths (relative to rag-evaluation-ragas/)
INPUT_DATASET=dataset-base/millbrook_dataset.json
OUTPUT_DATASET=output/millbrook_ragas_dataset.json
RESULTS_FILE=output/millbrook_ragas_results.json

# =============================================================================
# Usage
# =============================================================================
#
# 1. Copy this file: cp env.template .env
# 2. Edit .env with your values
# 3. Run any example:
#    - cd rag-mcp-chatbot && ./run_example.sh
#    - cd rag-evaluation-ragas && ./run_example.sh
#    - cd testAll && ./run_all.sh
#
# =============================================================================

