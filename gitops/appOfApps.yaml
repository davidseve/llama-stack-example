apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  destination:
    namespace: openshift-gitops
    server: 'https://kubernetes.default.svc'
  project: default
  source: 
    path: gitops/applications
    repoURL: 'https://github.com/davidseve/llama-stack-example.git'
    targetRevision: ragas-remote-v0.6
    helm:
      valueFiles:
      - ../values-common.yaml
      values: |  
        applications:
          rhoai:
            enabled: true
          discounts:
            enabled: true
          inference:
            enabled: false
          kubernetesMcp:
            enabled: true
          llamaStack:
            enabled: true
          milvus:
            enabled: true
          minio:
            enabled: true
          postgres:
            enabled: true 
          dspa:
            enabled: true
        providers:
          kubeflow:
            enabled: true
            # Internal service URL - pipeline pods can reach Llama Stack via cluster DNS
            llamaStackUrl: "http://llama-stack-example-service.llama-stack-example.svc.cluster.local:8321"     
        secret:
          data:
            LLAMA_4_SCOUT_API_TOKEN: ${LLAMA_4_SCOUT_API_TOKEN}
            KUBEFLOW_PIPELINES_TOKEN: ${KUBEFLOW_PIPELINES_TOKEN}
            AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
            AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
            AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
